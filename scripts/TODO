Create scripts to configure a zbackup server with:
    chroots
    secure write only file system
    file system quotas

Finish restore functionality to allow user to pick a remote backup
Set backup process priority low
